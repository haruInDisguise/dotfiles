#!/usr/bin/bash

# TODO: Maybe remove bashim's in the future?
# Launch Game: osu!

set -e

readonly GAME_EXEC_PATH='/mnt/data/games/osu.AppImage'

readonly TABLET_AREA_SCALE='33'
readonly TABLET_ID='Wacom Intuos PT S 2 Pen stylus' # use string as id might change trough kernel/driver update
readonly TABLET_MONITOR='PRIMARY'
readonly TABLET_UNMAP_BUTTONS='ALL'

# Tablet stuff is currently handled by the osu client using OpenTables fancyness

$GAME_EXEC_PATH

