-- config for: https://github.com/kyazdani42/nvim-web-devicons
-- just using the default for now
return {
    'nvim-web-devicons',
    event = 'VeryLazy',
    opts = {
        color_icons = true,
    }
}
